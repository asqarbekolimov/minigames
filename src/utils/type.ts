export interface GameCardI {
  cardImage: string;
  category: string;
  featured: boolean;
  likesCount: number;
  name: string;
  price: string | number;
  rating: number;
  shortDescription: string;
  slug: string;
}
